const initialState = {
  items: [],
  loading: false,
  error: null,
};

// const initialState = {
//   UserData: {},
// };

// export default (state = initialState, action) => {
//   switch (action.type) {
//     case "FETCH_USERDATA":
//       return {
//         ...state,
//         UserData: action.payload,
//       };
//     default:
//       return state;
//   }
// };
